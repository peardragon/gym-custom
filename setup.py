from setuptools import setup

setup(name='gym_custom', # name of the package
      version='0.0.1', # version of this release
      install_requires=['gym', 'ray'] # specifies the minimal list of libraries required to run the package correctly
)